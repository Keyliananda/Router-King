# Process Note 2026-03-14 12:30

## Scope

Dieser Nachtrag dokumentiert den naechsten offenen Schritt nach dem Sender-Fix fuer `Streaming + Disconnect`.
Der Fokus lag diesmal auf dem sichtbaren Dock-Pfad in `RouterKing/ui/main_dock.py`: unerwarteter Disconnect waehrend aktivem Job soll in der UI direkt abgesichert sein.

## Enthaltene Aenderungen

### Dock UI / Disconnect waehrend aktivem Job

- `tests/test_main_dock.py` erweitert:
  - Neuer Test fuer `_drain_sender()` bei unerwartetem Disconnect mit aktivem vorherigem Verbindungszustand.
  - Der Test prueft den echten UI-Pfad `poll -> _drain_sender() -> _apply_disconnected_state()`.
- Abgedeckte Erwartungen:
  - `Connection: disconnected`
  - `Machine: n/a`
  - `Job: idle`
  - `Connect`-Button und `Explore Limits`-Button gehen sichtbar in den Disconnect-/Idle-Zustand zurueck.
  - `Start`, `Pause` und `Stop` sind nach Disconnect deaktiviert.
  - `_explore_tick()` und Status-Requests laufen in diesem Abbruchpfad nicht weiter.

## Verifikation

Ausgefuehrt:

```bash
python3 -m unittest tests.test_main_dock tests.test_grbl_sender tests.test_gcode_parser
./scripts/test.sh
```

Ergebnis:

- Alle gezielten Tests gruen.
- Gesamtes Testskript ebenfalls gruen.

## Offene Punkte

- Naechster sinnvoller Maschinenpfad-Schritt waere eine UI-nahe Absicherung fuer `Pause/Resume/Abort` bei spaeten Serial-Fehlern oder Soft-Reset-Rueckmeldungen waehrend eines laufenden Streams.
