# Process Note 2026-03-14 13:40

## MCP-Implementierung Runde 2: Domain Tools, Dev Fallback, Doku

Offene Task-Pakete 6, 8, 9, 10, 11 aus der MCP-Roadmap abgearbeitet.

### Aenderungen

- **Task 6 — Spezialisierte Domain Tools:** 4 neue MCP-Tool-Wrapper in `mcp/server/routerking_tools.py`: `routerking_analyze_selection`, `routerking_optimize_splines_preview`, `routerking_generate_gcode`, `routerking_cam_generate_job`. Bauen Action-Payloads und rufen `routerking_apply_actions` auf, mit `capture_view=True` wo sinnvoll.

- **Task 8 — Machine Status:** Alle Machine-Tool-Wrapper in `mcp/server/machine_tools.py` nutzen jetzt `include_context=True`, damit der Agent nach connect/jog/stream/stop den Maschinenstatus sieht.

- **Task 9 — Dev Fallback:** `routerking_run_script` implementiert. Bridge-Methode in `RouterKing/mcp/bridge.py` mit `exec()` in isoliertem Namespace und stdout-Capture. Tool-Wrapper mit `RISK_DANGEROUS_DEV` Safety-Gate, nur aktiv bei `ROUTERKING_MCP_DEV_TOOLS=1`.

- **Task 10 — Tests:** 7 neue Tests (18 gesamt, alle bestanden): Domain-Tool-Payloads, None-Filterung, capture_view, run_script Ausfuehrung und Fehlerbehandlung, dangerous_dev Safety-Gate.

- **Task 11 — Dokumentation:** `docs/concepts/ai/MCP_TOOLS.md` erweitert (Server-Start, Env-Variablen, CLI-Flags, alle neuen Tools). `docs/concepts/ai/WORKBENCH_API.md` neu erstellt (Action-Registry-Tabelle, Risikoklassen, Confirm-Regeln).

### Noch offen

- Externer RPC-/Socket-Transport (aktuell nur Embedded-Mode)
- Strukturierte Maschinen-Statusdaten aus GRBL (Position, State)
