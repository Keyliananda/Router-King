# Process Note 2026-03-14 12:35

## Scope

Dieser Nachtrag dokumentiert den naechsten Maschinenpfad-Fix nach `87e5f7d925ca6140e79c17d278471a5e7011ac51`.
Der Fokus lag auf `Streaming + Disconnect`: Bei verlorenem Serial-Link sollten Sender-Status und Job-Fortschritt nicht als stale Altzustand im UI haengenbleiben.

## Enthaltene Aenderungen

### GRBL Sender / Streaming Disconnect

- `RouterKing/grbl/sender.py` angepasst:
  - `disconnect()` setzt beim Trennen jetzt nicht nur Streaming-Flags, sondern auch Job-Fortschritt und letzten Maschinenstatus zurueck.
  - `_mark_connection_lost()` verwendet denselben Reset fuer laufende Streams bei unerwartetem Serial-Fehler.
- Ergebnis:
  - Nach Linkverlust waehrend eines aktiven Streams bleiben keine veralteten `total/sent/acked`-Werte mehr stehen.
  - `get_status()` liefert nach Disconnect keinen stale `Idle`-/`Run`-Status mehr aus einer alten Verbindung.
  - Die Dock-UI kann dadurch nach `_apply_disconnected_state()` wieder konsistent `Job: idle` und den echten Disconnect-Zustand zeigen.

### Tests

- `tests/test_grbl_sender.py` erweitert:
  - Deckt jetzt explizit ab, dass manueller Disconnect laufende Job-/Statusdaten zuruecksetzt.
  - Deckt jetzt explizit ab, dass ein Serial-Abbruch waehrend aktivem Stream ebenfalls Status und Fortschritt bereinigt.

## Verifikation

Ausgefuehrt:

```bash
python3 -m py_compile RouterKing/grbl/sender.py tests/test_grbl_sender.py
python3 -m unittest tests.test_grbl_sender tests.test_main_dock
```

Ergebnis:

- Alle gezielten Sender-/Dock-Tests gruen.

## Offene Punkte

- Sinnvoller naechster Schritt: expliziter Dock-Test fuer `_drain_sender()` mit unerwartetem Disconnect waehrend aktivem Job, damit der sichtbare UI-Pfad `sender -> _drain_sender() -> _apply_disconnected_state()` direkt abgesichert ist.
