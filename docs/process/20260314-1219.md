# Process Note 2026-03-14 12:19

## Scope

Dieser Nachtrag dokumentiert den ersten Follow-up nach Commit `748c37441984e9f8f8983c33e0952d3c15ab954b`.
Der Fokus lag auf einem UI-State-Fix im GRBL-/Maschinenpfad nach Verbindungsverlusten waehrend `Explore Limits`.

## Enthaltene Aenderungen

### GRBL UI / Disconnect Handling

- `RouterKing/ui/main_dock.py` erweitert:
  - Neuer Helfer `_reset_explore_state()`, der den kompletten Explore-Laufzustand zentral zuruecksetzt.
  - Manueller Stopp von `Explore Limits` verwendet jetzt denselben Reset-Pfad statt nur Teil-Flags zurueckzunehmen.
  - `_apply_disconnected_state()` setzt den Explore-State jetzt ebenfalls vollstaendig zurueck.
- Ergebnis:
  - Nach manuellem Disconnect oder unerwartetem Serial-Linkverlust bleibt die UI nicht mehr im stale `Stop Explore`-Zustand haengen.
  - Maschinen- und Explore-Buttons werden nach Disconnect wieder konsistent aus dem echten Sender-/UI-Zustand berechnet.

### Tests

- `tests/test_main_dock.py` neu:
  - Importiert `RouterKing.ui.main_dock` mit lokalen FreeCAD-/PySide-/Serial-Stubs.
  - Prueft, dass `_apply_disconnected_state()` bei aktivem Explore-Lauf alle relevanten Explore-Flags und UI-Elemente sauber zuruecksetzt.

## Verifikation

Ausgefuehrt:

```bash
python3 -m py_compile RouterKing/ui/main_dock.py tests/test_main_dock.py
python3 -m unittest tests.test_main_dock tests.test_grbl_sender tests.test_gcode_parser
./scripts/test.sh
```

Ergebnis:

- Alle gezielten Tests gruen.
- Gesamtes `./scripts/test.sh` ebenfalls gruen.

## Offene Punkte

- Naechster Hebel im Maschinenpfad: explizite Absicherung von `Streaming + Disconnect` auf UI-Ebene, damit Jobstatus und Bedienelemente auch waehrend aktivem G-Code-Stream sauber auf Linkverlust reagieren.
- Die AI-Action-Schicht bleibt weiterhin ohne echte End-to-End-Session in FreeCAD abgesichert.
